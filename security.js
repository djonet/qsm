// 🛡️ security.js — تنبيه أمني بسيط يظهر مرة واحدة فقط عند أول دخول للموقع

(function () {
  const shownKey = "__warning_shown_once";

  // إذا شاهد المستخدم التنبيه من قبل فلا يظهر مجددًا
  if (localStorage.getItem(shownKey)) {
    console.log("✅ التحذير الأمني عُرض مسبقًا، لن يظهر مجددًا.");
    return;
  }

  // إظهار التنبيه مرة واحدة فقط
  alert(
    "🚫 تحذير أمني مهم!\n\n" +
    "يُمنع تمامًا مشاركة الحساب أو التقاط الشاشة أو نشر محتوى الموقع بأي وسيلة.\n" +
    "أي مخالفة لذلك ستؤدي إلى إغلاق الحساب نهائيًا."
  );

  // حفظ أنه تم عرض التنبيه
  localStorage.setItem(shownKey, "true");
})();
